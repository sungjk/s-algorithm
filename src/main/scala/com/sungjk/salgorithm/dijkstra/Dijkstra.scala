package com.sungjk.salgorithm.dijkstra

import com.sungjk.salgorithm.common.GlobalGraph.{Distance, Graph, Node, Path}

object Dijkstra {
	def shortestPath(start: Node, end: Node, graph: Graph): Path = ???
	def shortestDistance(start: Node, end: Node, graph: Graph): Distance = ???
}
